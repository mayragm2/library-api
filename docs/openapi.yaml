# Contrato de la Library API, en formato OpenAPI 3.0.
# Se sirve en http://localhost:3000/docs con Swagger UI. Guardá el archivo y recargá la página.
#
# Regla de toda la clase: el contrato de un endpoint se escribe ACÁ antes de programarlo.

openapi: 3.0.3

info:
  title: Library API
  version: 1.0.0
  description: |
    Books, authors and loans of a library.

    **Conventions**
    - All requests and responses are JSON.
    - Dates use the `YYYY-MM-DD` format.
    - All errors have the shape `{ "error": "..." }` (see the `Error` schema).

servers:
  - url: http://localhost:3000

# ──────────────────────────────────────────────────────────────
# Endpoints. Un bloque por ruta; dentro, un bloque por verbo.
# Empezá por /authors. Abajo hay una plantilla comentada para copiar.
# ──────────────────────────────────────────────────────────────
paths: 
  /authors:
    get:
      summary: gets a list of authors
      responses:
        "200":
          description: Resource found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Authors"
        "404":
          description: Resource not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                error: Resource not found
  /authors/{id}:
    get:
      summary: gets an author by its id
      responses:
        "200":
          description: Resource found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Author"
        "404":
          description: Resource not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                error: Resource not found
    delete:
      summary: deletes an author by its id
      responses:
        "204":
          description: author deleted
        "400":
          description: Bad Request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                error: Resource not found
        "404":
          description: Resource not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                error: Resource not found
        "409":
          description: author has books
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                error: Cannot delete author that has books
/books:
    get:
      summary: Search books with filters and pagination
      tags: [Books]
      parameters:
        - name: title
          in: query
          required: false
          schema: { type: string }
        - name: available
          in: query
          required: false
          schema: { type: boolean }
        - name: author_id
          in: query
          required: false
          schema: { type: integer }
        - name: page
          in: query
          required: false
          schema: { type: integer, minimum: 1, default: 1 }
        - name: limit
          in: query
          required: false
          schema: { type: integer, minimum: 1, default: 10 }
      responses:
        "200":
          description: Page of books
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BookPage"
        "400":
          description: Invalid query param
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      summary: Create a book
      tags: [Books]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NewBook"
      responses:
        "201":
          description: Book created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Book"
        "404":
          description: Author not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /books/{id}:
    get:
      summary: Get a book by id
      tags: [Books]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Book found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Book"
        "400":
          description: Id is not a number
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Book not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      summary: Update some fields of a book
      tags: [Books]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateBook"
      responses:
        "200":
          description: Book updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Book"
        "400":
          description: Id is not a number
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Book or author not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      summary: Replace all fields of a book
      tags: [Books]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NewBook"
      responses:
        "200":
          description: Book replaced
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Book"
        "400":
          description: Id is not a number
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Book or author not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    delete:
      summary: Delete a book (only if it was never loaned)
      tags: [Books]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "204":
          description: Book deleted
        "400":
          description: Id is not a number
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Book not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Book has loans
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
# Plantilla de un endpoint (descomentá, pegá dentro de `paths:` y completá):
#
#  /resource/{id}:
#    get:
#      summary: What it does, in one line
#      tags: [Resource]
#      parameters:
#        - name: id
#          in: path
#          required: true
#          schema: { type: integer }
#      requestBody:                       # solo en POST / PUT / PATCH
#        required: true
#        content:
#          application/json:
#            schema:
#              $ref: "#/components/schemas/NewResource"
#      responses:
#        "200":
#          description: Resource found
#          content:
#            application/json:
#              schema:
#                $ref: "#/components/schemas/Resource"
#        "404":
#          description: Resource not found
#          content:
#            application/json:
#              schema:
#                $ref: "#/components/schemas/Error"
#              example:
#                error: Resource not found

# ──────────────────────────────────────────────────────────────
# Schemas: la forma de los datos. Tienen que coincidir con las
# tablas de la base y con las interfaces de src/types/.
# ──────────────────────────────────────────────────────────────
components:
  schemas:
    Error:
      type: object
      required: [error]
      properties:
        error:
          type: string
          example: Book not found
    Author:
      type: object
      required: [id, name, nationality]
      properties:
        id:
          type: integer
          example: 1
        name:
          type: string
          example: Julian Kartun
        nationality:
          type: string
          example: Argentino
    Authors:
      type: array
      required: [data]
      items:
        data:
          type: Author
          example: [{id: 1, name: maria, nationality: argentine}, {id: 2, name: claudio, nationality: argentine}]
    
    NewBook:
      type: object
      required: [id, title, year]
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 200
          example: Harry Potter and the Philosopher's Stone
        year:
          type: integer
          minimum: 1000
          maximum: 2100
          example: 1991
        author_id:
          type: integer
          example: 2
    UpdateBook:
      type: object
      required: [id, title, year]
      properties:
        title:
          type: string
          example: Harry Potter and the Philosopher's Stone
        year:
          type: integer
          example: 1991
        author_id:
          type: integer
          example: 2
    Book:
      type: object
      required: [id, title, year, author_id, available]
      properties:
        id:
          type: integer
          example: 1
        title:
          type: string
          example: Harry Potter and the Philosopher's Stone
        year:
          type: integer
          example: 1991
        author_id:
          type: integer
          example: 2
        available:
          type: bool
          example: false

    BookPage:
      type: object
      properties: 
        data:
          type: Book[]
          example: [{id: 1, title: Harry Potter and the Philosopher's Stone, year: 1991, author_id: 2, available: false}]
        total:
          type: integer
          example: 650
        page:
          type: integer
          example: 5
        limit:
          type: integer
          example: 10
    Date:
      type: string
      description: YYYY-MM-DD
      example: 2026-04-02
    Loan:
      type: object
      required: [id, book_id, member_name, loan_date]
      properties:
        id:
          type: integer
          example: 1
        book_id:
          type: integer
          example: 5
        member_name:
          type: string
          example: Charles
        loan_date:
          type: Date
          example: 2025-12-13
        return_date:
          type: Date
          nullable: true
          example: 2026-09-12
    NewLoan:
      type: object
      required: [book_id, member_name]
      properties:
        book_id:
          type: integer
          example: 5
        member_name:
          type: string
          nullable: false
          example: Charles
